services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend/uploads:/app/backend/uploads
      - ./backend/outputs:/app/backend/outputs
      - ./backend/transt.db:/app/backend/transt.db
    env_file:
      - .env
    restart: unless-stopped
